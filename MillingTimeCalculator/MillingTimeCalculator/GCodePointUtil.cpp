#include "GCodePointUtil.h"

void CalcDistance(vector<GCode::CPoint*>& vPoint)
{
	for(size_t i = 0; i < vPoint.size() - 1; ++i)
	{
		const GCode::CPoint* P1 = vPoint[i];
		const GCode::CPoint* P2 = vPoint[i + 1];

	}
}